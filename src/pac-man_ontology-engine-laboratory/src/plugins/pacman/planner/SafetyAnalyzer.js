const DIRS={UP:[0,-1],DOWN:[0,1],LEFT:[-1,0],RIGHT:[1,0]},MOVES=Object.values(DIRS),key=(x,y)=>`${x},${y}`;
export class SafetyAnalyzer{
  analyze(ontology,direction,horizon=10){const maze=ontology.query({type:'Maze'})[0],p=ontology.query({type:'Player'})[0],[dx,dy]=DIRS[direction],start={x:p.x+dx,y:p.y+dy},shared=ontology.objects.get('threat-field')?.ghostArrival,ghostArrival=shared||Object.fromEntries(this.ghostArrivalMap(ontology,maze)),topology=ontology.objects.get('topology'),firstDanger=ghostArrival[key(start.x,start.y)]??Infinity;if(firstDanger<=1)return{direction,survival:0,safeArea:0,junctions:0,minGhost:firstDanger,score:-100000};let frontier=[start],seen=new Set([key(start.x,start.y)]),junctions=0,survival=1;
    for(let t=2;t<=horizon;t++){const next=new Map;for(const pos of frontier)for(const[mx,my]of MOVES){const x=pos.x+mx,y=pos.y+my,k=key(x,y),arrival=ghostArrival[k]??Infinity;if(maze.grid[y]?.[x]!==0||arrival<=t)continue;next.set(k,{x,y});seen.add(k)}frontier=[...next.values()];if(!frontier.length)break;survival=t;for(const pos of frontier){const k=key(pos.x,pos.y),isJunction=topology?.kindByCell?.[k]==='junction';if(isJunction)junctions++}}
    const minGhost=Math.min(...ontology.query({type:'Ghost'}).map(g=>Math.abs(g.x-start.x)+Math.abs(g.y-start.y))),startKey=key(start.x,start.y),startKind=topology?.kindByCell?.[startKey]||'unknown',bottlenecks=new Set((topology?.bottlenecks||[]).map(b=>key(b.x,b.y))),bottleneckAhead=[...seen].some(k=>bottlenecks.has(k)),trapRisk=(startKind==='corridor'&&junctions===0?2:0)+(bottleneckAhead?2:0),score=survival*1000+seen.size*8+junctions*3+minGhost*5-trapRisk*250;return{direction,survival,safeArea:seen.size,junctions,minGhost,startKind,bottleneckAhead,trapRisk,score}}
  ghostArrivalMap(ontology,maze){const dist=new Map,queue=ontology.query({type:'Ghost'}).map(g=>({x:g.x,y:g.y,d:0}));for(const g of queue)dist.set(key(g.x,g.y),0);for(let i=0;i<queue.length;i++){const cur=queue[i];for(const[dx,dy]of MOVES){const x=cur.x+dx,y=cur.y+dy,k=key(x,y);if(maze.grid[y]?.[x]!==0||dist.has(k))continue;dist.set(k,cur.d+1);queue.push({x,y,d:cur.d+1})}}return dist}
}
