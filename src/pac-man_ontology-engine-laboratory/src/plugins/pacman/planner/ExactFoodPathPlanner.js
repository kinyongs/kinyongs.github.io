const MOVES=[['UP',0,-1],['DOWN',0,1],['LEFT',-1,0],['RIGHT',1,0]],key=(x,y)=>`${x},${y}`;
export class ExactFoodPathPlanner{
  plan(ontology,{targetKeys=new Set,recentKeys=[],preferredId=null}={}){const maze=ontology.query({type:'Maze'})[0],player=ontology.query({type:'Player'})[0],all=ontology.query({where:o=>o.type==='Pellet'||o.type==='PowerPellet'}),remaining=all.length,candidates=remaining<=5?all:all.filter(f=>targetKeys.has(key(f.x,f.y))),preferred=candidates.find(f=>f.id===preferredId),targets=preferred?[preferred]:candidates;if(!targets.length)return{target:null,path:[],direction:null,distance:Infinity,remaining};const targetMap=new Map(targets.map(f=>[key(f.x,f.y),f])),ghosts=ontology.query({type:'Ghost'}),field=ontology.objects.get('threat-field'),recentCount=new Map(recentKeys.map(k=>[k,(recentKeys.filter(x=>x===k).length)])),startKey=key(player.x,player.y),cost=new Map([[startKey,0]]),previous=new Map,queue=[{x:player.x,y:player.y,cost:0}];let goal=null;
    while(queue.length){queue.sort((a,b)=>a.cost-b.cost);const cur=queue.shift(),ck=key(cur.x,cur.y);if(cur.cost!==cost.get(ck))continue;if(targetMap.has(ck)){goal=cur;break}for(const[direction,dx,dy]of MOVES){const x=cur.x+dx,y=cur.y+dy,k=key(x,y);if(maze.grid[y]?.[x]!==0)continue;const ghostDistance=field?.ghostArrival?.[k]??Math.min(...ghosts.map(g=>Math.abs(g.x-x)+Math.abs(g.y-y))),danger=Math.max(0,6-ghostDistance)*2.8,repeat=(recentCount.get(k)||0)*5,next=cur.cost+1+danger+repeat;if(next<(cost.get(k)??Infinity)){cost.set(k,next);previous.set(k,{from:ck,direction,x,y});queue.push({x,y,cost:next})}}}
    if(!goal)return{target:null,path:[],direction:null,distance:Infinity,remaining};const path=[];let cursor=key(goal.x,goal.y);while(cursor!==startKey){const step=previous.get(cursor);if(!step)break;path.unshift({direction:step.direction,x:step.x,y:step.y});cursor=step.from}return{target:targetMap.get(key(goal.x,goal.y)),path,direction:path[0]?.direction||null,distance:path.length,cost:goal.cost,remaining,mode:remaining<=5?'FINAL PELLET HUNT':'CLUSTER TARGET'}}
}
